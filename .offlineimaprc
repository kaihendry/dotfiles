[general]
accounts = UnivCS

[Account UnivCS]
localrepository = LocalCS
remoterepository = RemoteCS

[Account Bilbo]
localrepository = LocalBilbo
remoterepository = RemoteBilbo

[Repository LocalCS]
type = Maildir
localfolders = ~/Mail/UnivCS

[Repository LocalBilbo]
type = Maildir
localfolders = ~/Mail/Bilbo

[Repository RemoteCS]
type = IMAP
ssl = yes
remotehost = mail.cs.helsinki.fi
remoteuser = hendry

[Repository RemoteBilbo]
type = IMAP
ssl = yes
remotehost = daidalos.pannuhuone.org
remoteuser = hendry

##################################################
# Mailbox name recorder
##################################################

[mbnames]

# offlineimap can record your mailbox names in a format you specify.
# You can define the header, each mailbox item, the separator,
# and the footer.  Here is an example for Mutt.
# If enabled is yes, all six setting must be specified, even if they
# are just the empty string "".
#
# The header, peritem, sep, and footer are all Python expressions passed
# through eval, so you can (and must) use Python quoting.

enabled = yes
filename = ~/mail/muttrc.mailboxes
header = "mailboxes "
peritem = "+%(accountname)s/%(foldername)s"
sep = " "
footer = "\n"
